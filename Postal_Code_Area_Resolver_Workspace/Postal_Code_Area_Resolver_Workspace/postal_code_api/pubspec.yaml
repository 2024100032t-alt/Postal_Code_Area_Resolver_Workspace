name: postal_code_api
description: API client for resolving ZIP/postal codes via Zippopotam.us.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '^3.0.0'

dependencies:
  http: ^1.2.0
  logging: ^1.2.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0