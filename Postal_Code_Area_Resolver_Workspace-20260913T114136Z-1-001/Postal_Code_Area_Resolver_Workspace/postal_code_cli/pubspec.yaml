name: postal_code_cli
description: CLI application for Postal Code Area Resolver.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '^3.0.0'

dependencies:
  http: ^1.2.0
  logging: ^1.2.0
  terminal_colors:
    path: ../terminal_colors
  postal_code_api:
    path: ../postal_code_api

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0